[build]
  # Directory where your build outputs (the dist folder from Vite)
  publish = "dist"
  
  # Build command
  command = "npm run build"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
